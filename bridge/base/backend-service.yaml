# check if there is at least one published port

#! backend-service.yaml
# Generated code, do not edit
apiVersion: v1
kind: Service
metadata:
    name: backend-published
    namespace: short-url
    labels:
        com.docker.compose.project: short-url
        com.docker.compose.service: backend
spec:
    selector:
        com.docker.compose.project: short-url
        com.docker.compose.service: backend
    ports:
        - name: backend-8001
          port: 8001
          protocol: TCP
          targetPort: backend-8001

# check if there is at least one published port
